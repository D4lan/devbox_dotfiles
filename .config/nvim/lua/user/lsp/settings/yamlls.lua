return {
    settings = {
        yaml = {
            format = {
                enable = true,
                singleQuote = true,
                printWidth = 120,
            },
            hover = true,
            completion = true,
            validate = true,
            schemaStore = {
                url = "https://www.schemastore.org/json",
                enable = true,
            }
        }
    }
}
